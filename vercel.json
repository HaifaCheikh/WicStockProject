{
  "buildCommand": "curl -sSL https://dot.net/v1/dotnet-install.sh | bash -s -- --channel 8.0 --install-dir $HOME/.dotnet && $HOME/.dotnet/dotnet publish frontend/WicStock.Web.csproj -c Release -o output_vercel",
  "outputDirectory": "output_vercel/wwwroot",
  "routes": [
    {
      "src": "/uploads/(.*)",
      "dest": "https://wicstock-api.onrender.com/uploads/$1"
    },
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
