global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "wicstock-backend"
    metrics_path: "/metrics"
    static_configs:
      - targets: ["api:8080"]
        labels:
          application: "wicstock-api"
          environment: "local"
