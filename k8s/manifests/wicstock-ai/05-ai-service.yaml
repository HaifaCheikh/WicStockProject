apiVersion: v1
kind: Service
metadata:
  name: ai-service
  namespace: wicstock-ai
  labels:
    app: ai-service
    app.kubernetes.io/part-of: wicstock
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: ai-service
